Run IAP (In-App Purchase) quality assurance checks.

1. Run `pnpm qa:iap:static` to execute static IAP analysis
2. Review any warnings or errors in the output
3. If issues are found, analyze them and suggest fixes
4. Cross-reference with `docs/QA_IAP_SCENARIOS.md` for expected behavior
5. Report a summary of IAP health status
